#include <imgui.h>
#include "asset_tabs.h"
#include "data/scene_data.h"
#include "gui/helper.h"

namespace editor::gui::asset_tab {
    void animation() {
        PREFIX_AS(animation)
        SUFFIX_AS
    }

    void audio() {
        PREFIX_AS(audio)
        SUFFIX_AS
    }

    void font() {
        PREFIX_AS(font)
        SUFFIX_AS
    }

    void texture() {
        PREFIX_AS(texture)
        SUFFIX_AS
    }
}